{
  "name": "git-auto-tag-hook-v2",
  "version": "2.0.0",
  "type": "module",
  "description": "Claude Code hook for auto git commit and tagging (V2 - improved security and logging)",
  "private": true,
  "scripts": {
    "build": "bun build src/hook.ts --outdir dist --target node",
    "test": "node test/test.js"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.0.0"
  }
}
